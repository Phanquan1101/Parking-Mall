# Shared Contracts

Current status: Placeholder only.

The API source of truth is currently [`docs/API_CONTRACT.md`](../../docs/API_CONTRACT.md). Future OpenAPI exports and generated TypeScript/Java types belong in `openapi/` and `types/` after their source contracts are stable.
