name: parkflow-mall

services:
  identity-service:
    build:
      context: ./services/identity-service
    environment:
      IDENTITY_SERVICE_PORT: 8081
      JWT_SECRET: ${JWT_SECRET:-parkflow-local-development-secret-change-me-2026}
      JWT_EXPIRATION_SECONDS: ${JWT_EXPIRATION_SECONDS:-3600}
    ports:
      - "${IDENTITY_SERVICE_PORT:-8081}:8081"

  api-gateway:
    build:
      context: ./services/api-gateway
    depends_on:
      - identity-service
      - parking-service
    environment:
      API_GATEWAY_PORT: 8080
      IDENTITY_SERVICE_BASE_URL: http://identity-service:8081
      PARKING_SERVICE_BASE_URL: http://parking-service:8082
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-http://localhost:5173}
    ports:
      - "${API_GATEWAY_PORT:-8080}:8080"

  parking-service:
    build:
      context: ./services/parking-service
    environment:
      PARKING_SERVICE_PORT: 8082
      JWT_SECRET: ${JWT_SECRET:-parkflow-local-development-secret-change-me-2026}
      PUBLIC_BASE_URL: http://localhost:${API_GATEWAY_PORT:-8080}
    ports:
      - "${PARKING_SERVICE_PORT:-8082}:8082"

# Slice 2 contains no database, Supabase, SePay, OCR model, or other domain services.
