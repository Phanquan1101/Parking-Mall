const base=(import.meta.env.VITE_API_BASE_URL??"http://localhost:8080").replace(/\/$/,"");
export type OcrResult={ocrRequestId:string;candidatePlate:string|null;normalizedCandidatePlate:string|null;confidence:number;provider:string;warnings:string[];createdAt:string};
export async function recognizePlate(token:string,file:File){const form=new FormData();form.append("image",file);const response=await fetch(base+"/api/vision/ocr/plate",{method:"POST",headers:{Authorization:`Bearer ${token}`},body:form});if(!response.ok)throw new Error((await response.text())||"OCR request failed");return response.json() as Promise<OcrResult>}
export async function ocrAssistedCheckIn(token:string,body:Record<string,unknown>){const response=await fetch(base+"/api/parking/sessions/check-in",{method:"POST",headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body:JSON.stringify(body)});if(!response.ok)throw new Error((await response.text())||"Check-in failed");return response.json() as Promise<{sessionId:string;sessionCode:string;qrLookupToken:string;ticketUrl:string}>}
