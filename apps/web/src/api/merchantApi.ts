const base=(import.meta.env.VITE_API_BASE_URL??"http://localhost:8080").replace(/\/$/,""); export type MerchantResult={validationId:string;sessionId:string;sessionCode:string;invoiceAmount:number;totalEligibleInvoiceAmount:number;discountAmount:number;finalFee:number;status:string;message:string}; export async function validateInvoice(token:string,lookupToken:string,invoiceCode:string,invoiceAmount:number){const r=await fetch(base+"/api/merchant/validations",{method:"POST",headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body:JSON.stringify({lookupToken,invoiceCode,invoiceAmount})});if(!r.ok)throw new Error(await r.text()||"Invoice validation failed");return r.json() as Promise<MerchantResult>;}
