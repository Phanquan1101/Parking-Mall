import {useEffect,useState} from "react";import {useParams} from "react-router-dom";import {cancelReservation,getReservation,type Reservation} from "../api/reservationApi";
export function ReservationPage(){const {reservationCode=""}=useParams();const [data,setData]=useState<Reservation|null>(null);const [error,setError]=useState("");useEffect(()=>{void getReservation(reservationCode).then(setData).catch(e=>setError(e instanceof Error?e.message:"Reservation not found"));},[reservationCode]);async function cancel(){try{setData(await cancelReservation(reservationCode,"Customer cancelled reservation."));}catch(e){setError(e instanceof Error?e.message:"Unable to cancel reservation");}}if(error)return <main className="ticket-layout"><section className="state-panel state-panel--error"><h1>Reservation unavailable</h1><p>{error}</p></section></main>;if(!data)return <main className="ticket-layout"><section className="state-panel"><h1>Loading reservation…</h1></section></main>;return <main className="ticket-layout"><section className="ticket-intro"><span className="ticket-code">Reservation code</span><h1>{data.reservationCode}</h1><p>Show this code to parking staff at check-in.</p></section><section className="ticket-sheet"><div className="ticket-section"><dl className="details-grid"><div><dt>Status</dt><dd><span className="status">{data.status}</span></dd></div><div><dt>Vehicle</dt><dd>{data.vehiclePlate} · {data.vehicleType}</dd></div><div><dt>Reserved from</dt><dd>{new Date(data.reservedFrom).toLocaleString()}</dd></div><div><dt>Reserved until</dt><dd>{new Date(data.reservedUntil).toLocaleString()}</dd></div></dl>{data.status==="RESERVED"&&<button className="primary-button" onClick={()=>void cancel()}>Cancel reservation</button>}</div></section></main>}
